<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<section>
    <% @tasks.each do |task|%>
  <li class="title">
    <%= "#{task[:name]}" %>
  </li>
  <li>
    <%= "#{task[:completion]}" %>
  </li>
  <li>
    <%= "#{task[:completion_date]}" %>
  </li>
    <% end %>
</section>
