<!-- <h1> Edit a task!</h1>

<%= form_with model: @task, class: "task-edit-form" do |f| %>
  <p>Please edit the field of your taks:</p>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.label :completion_date %>
  <%= f.text_field :completion_date %>

  <%= f.submit "Save Edit", class: "save-edits-button" %>
<% end %> -->

<%= render partial: "form", locals: { action_name: "Edit Task", button_title: 'Save Edit' } %>
